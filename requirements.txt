# =============================================================================
# Install CPU-only PyTorch instead of the default GPU wheel.
# The default wheel pulls ~6 GB of nvidia-*-cu12 runtime libraries on Linux,
# which are pure dead weight on Render's CPU-only instances.
# =============================================================================
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Django core ---
Django==6.0.1
gunicorn==23.0.0
uvicorn[standard]>=0.30
whitenoise==6.8.2
django-allauth==65.14.3
django-cors-headers==4.9.0
django-environ==0.12.0
dj-database-url>=2.1.0
psycopg2-binary>=2.9.9

# --- Crypto / HTTP ---
cryptography==46.0.5
PyJWT==2.10.1
requests==2.32.5

# --- External services ---
pusher>=3.3.4
google-genai==1.66.0
openai==2.24.0
neo4j==5.28.1

# --- Image upload (OCR handled by Gemini multimodal, see app/services/ocr.py) ---
pillow==12.1.0

# --- Embeddings (ACE runtime, memory search, agent/skill catalogs) ---
sentence-transformers>=3.0.0

# --- PyTorch (CPU-only; +cpu local version is published on the CPU index) ---
torch==2.10.0+cpu

# --- Scientific stack ---
numpy==2.4.2
scikit-learn>=1.5.0

# --- Charts (chat analytics) ---
matplotlib==3.10.6

# --- Small runtime utilities ---
python-dotenv==1.2.2
psutil==7.1.3
Markdown==3.7
sqlparse==0.5.5
